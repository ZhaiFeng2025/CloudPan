# 任务批次 5（2026-08-03）

> 由 contract/ (schemaVersion=3) 渲染生成，禁止手改。

## 批次结论

v1.1.3 四维审查（批次 5）：0 P0 / 4 P1，共 54 条。P1 优先排入：目录删除传播断裂（服务端 prefix 带尾斜杠与生产无尾斜杠约定冲突，目录自身条目永不墓碑化→空目录幽灵永生，客户端 FullScan 又只对 File 快照补删除检测，目录删除无兜底，且被带尾斜杠单测掩盖）；Android 删除仍物理删除无回收站（批次 4 截断项，服务端 /api/trash 三端点已就绪，纯客户端缺口）；提交新版本编排 Upload/ChunkedUpload 双份（7.1 高危 DB+FS 一致性代码最不值得双份）；进程执行辅助 4 处近似重复（命令注入安全敏感区）。P2 覆盖：T-041 回炉（SettingsForm 测试连接仍手建 HttpClient+魔数 /api/health，且手建客户端无证书回退与真实连接行为分叉，成功弹模态/失败裸错误）；选择性同步排除集只拦下载不拦上传（排除目录成单向发布源，隐私文件外传）+ 重新勾选永久卡 CloudOnly（T-047 未闭环）；ServerWindow 直连 DbContext 绕过 Core（服务定位器注入）；FilesController.Upload 冲突策略内嵌（与分块上传已下沉先例矛盾，行为分叉）；WebSocket 心跳裸 Timer 散落领域服务；服务端 16 处 Controller 路由字面量未用 SpecRoutes + /ws 未契约化（T-027 只收敛客户端半边）；Android 列表固定 limit=100 静默截断；照片备份队头阻塞（单张坏照片全相册停摆）。任务依赖：T-054 与 T-049 均改 SyncEngine.FullScan/Scan，T-051 与 T-056 均触及上传事务，执行端应顺序处理避免并行合并冲突。冲突取舍：①T-051 先收敛提交新版本编排、T-056 再下沉 Upload 冲突策略，避免在重复编排上叠加下沉返工；②T-050/T-059/T-060 均改 Android FileListScreen/PhotoBackupWorker，建议同执行批次顺序处理。截断说明：12 任务配额满——Android Kotlin 生成器、客户端持久化落位 Infrastructure、行数门禁 partial 聚合（SyncEngine 2397/MainWindow 2793）、ThemeWatcher 双份、DesignTokens 死令牌清理、WebSocket 重连退避契约化、TokenService 服务定位器解耦、Contract 工具下沉、UI 桥反向边、请求体 DTO 契约化、Program 静态全局状态、IApiClient 签名生成、ValidatePath 残留、DeleteForTrashAsync 残留队列项、CloudOnly 磁盘检查、分块 version=0、KeepBoth 冲突后缀、重命名撞名、删除 409、浏览卡顿、二维码配对、设置即时生效、Windows 多选、分享页模板化、设备名映射、托盘死端、弹窗主题、状态色对比度、时间排序、网格缩略图、Android 底部导航等 P2/P3 进批次 6。

## 维度摘要

| 维度 | 本批次 |
|------|--------|
| 架构 | P2×3 |
| 功能 | P1×1 P2×3 |
| UX | P1×1 |
| 技术简洁 | P1×2 P2×2 |

## 问题清单（本批次 findings）

### F-49 [功能 / P1] CloudPan.Server.Core/FileIndex/FileIndexService.cs:129-136; CloudPan.Client.Core/Services/SyncEngine.FullScan.cs:100-118; CloudPan.Tests/Server/Services/FileIndexServiceTests.cs:88-109

- **问题**：服务端 SoftDeleteAsync 对目录用 prefix = path.TrimEnd('/') + "/" 做 StartsWith 匹配，但生产环境目录条目按 T-046 约定无尾斜杠存储（MkdirAsync 显式去掉尾斜杠、客户端入队路径均无尾斜杠），"/dir" 不以 "/dir/" 开头 → 目录自身 FileEntry 永远不被标记 Deleting，只有后代被墓碑化；单测用 CreateDirectoryAsync("/parent/",1) 带尾斜杠创建掩盖了与生产不一致。客户端 FullScan 删除检测仅对 File&&IsDownloaded 入队 Delete，目录删除从不被 5 分钟兜底扫描发现。后果：设备 A 删除目录后，设备 B 只收到后代 Deleting，空目录永远残留；服务端 /dir 条目以 Synced 状态永驻，删除后同名重建 MkdirAsync 命中已存在路径返回 409。
- **为何阻碍使命**：目录级删除是 T-005 删除传播声明覆盖的核心路径，『删除后其他设备清掉副本』的收敛承诺对目录失效，空目录幽灵在全部设备与索引中永生，且被与生产不一致的测试掩盖，违背『多设备收敛/删除可恢复』判据。

### F-50 [UX / P1] CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt:128-133; ui/FileListScreen.kt:210-258

- **问题**：Android 删除确认框（FileListScreen.kt:210-228）与批量删除（showBulkDeleteDialog，行 233-258）均直接调 /api/files DELETE 物理删除，无回收站、无撤销 Snackbar、无最近删除入口；Windows 端已落地软删+撤销条+回收站三端点（T-014），Android 完全未复用服务端 /api/trash。
- **为何阻碍使命**：手机是家庭拍照与误触高发设备，物理删除违反『删除进回收站而非物理删除』与 Nielsen 用户控制与自由；服务端回收站三端点已就绪，是纯客户端缺口，是本次批次中最刺痛家庭用户的项（批次 4 截断项）。

### F-51 [技术简洁 / P1] CloudPan.Server.Core/Upload/UploadService.cs:52-76 vs CloudPan.Server.Core/ChunkedUpload/ChunkedUploadService.Finalize.cs:86-133

- **问题**：存档旧版本(StoreVersionAsync) → 添加 VersionRecord → 裁剪超 MaxVersionsDefault(RemoveRange) → 更新/upsert FileEntry 的『提交新版本』编排在 UploadService 与 ChunkedUploadService.Finalize 各写一份，事务边界、回滚语义与 FS 副作用清理各实现一遍（均属 7.1 高危 DB+FS 一致性代码）。批次 4 只修单点缺陷，未消除编排重复。
- **为何阻碍使命**：一致性关键逻辑多处复制，任一处修订遗漏即引入索引与磁盘不一致缺陷（正是 T-001 曾修复的根因区）；高一致性代码最不值得双份，审阅者必须同时核对两处语义。

### F-52 [技术简洁 / P1] CloudPan.Server.UI/ServerInstaller.Process.cs:13-121; CloudPan.Server.UI/ServiceRestartHelper.cs:69-127

- **问题**：进程执行辅助四处近似重复：RunExe（ArgumentList 安全路径）与 RunCmd（cmd 管道）结构相同（启动→WaitForExit 30s→Kill→收集 stderr→错误截断 120 字符），ServiceRestartHelper 又自写 RunElevatedSc（runas+WaitForExit 30s）与 ServiceHasLegacyBinPathParam（sc qc 读取+WaitForExit 5s），散落在 4 个方法、2 个文件，各自携带 try/catch 与超时样板。
- **为何阻碍使命**：命令执行是安全敏感区（7.6 反模式 5 明示命令注入），多份实现使参数转义/超时/错误收集语义各自演化，审阅与维护成本高，收敛为单点更易审计。

### F-53 [技术简洁 / P2] CloudPan.Client.UI/UI/SettingsForm.AccountTab.cs:229-271

- **问题**：测试连接仍手建 new HttpClient{Timeout=5s} 并拼魔数 "/{url}/api/health"，绕过 T-041『客户端 HTTP 单一入口』承诺；且手建 HttpClient 未配置证书回退，对自签证书服务端测试连接报证书错误而实际同步走 ApiClient 正常，行为分叉。UX 侧成功弹模态 MessageBox 打断，失败透出裸状态码（404）与底层异常原文，无白话原因与下一步。
- **为何阻碍使命**：T-041 已标记完成但被 UI 层绕开，同一 HttpClient 装配的契约收敛被撕开第二入口，测试结果与真实连接不一致误导用户；成功弹窗与裸错误违反『错误=原因+下一步』与『删减优先』修复取向。

### F-54 [功能 / P2] CloudPan.Client.Core/Services/SyncEngine.Scan.cs:172,188; SyncEngine.FullScan.cs:92-98,166,178; FileWatcherService.cs:106,111

- **问题**：T-047 排除集语义只拦截下载方向：IsPathSelected 仅在 ApplyRemoteChangesAsync（下载）被检查（Scan.cs:172），上传方向从不检查——FullScan 第 3 步对无快照本地文件直接入队 Upload（FullScan.cs:166,178），FileWatcherService.OnChanged 对新建/修改文件同样直接入队上传。用户取消勾选 /Private 后目录内新建/修改文件仍被推上云端；重新勾选后 Scan.cs:188 版本相等分支不重置 State，CloudOnly 永久滞留，本地改动被 cloudOnlyPaths 跳过永不重新上传。
- **为何阻碍使命**：『排除』对用户意味着该目录不参与双向同步，现状是排除目录变成该设备单向发布源，把用户明确不想外传的隐私文件（相册/证件扫描）推上云端，违背『拒绝意外外传』；重新勾选不可逆是功能半途状态，违背『改动即时生效/多设备收敛』。

### F-55 [架构 / P2] CloudPan.Server.UI/ServerWindow.cs:23,37; ServerWindow.Data.cs:19

- **问题**：ServerWindow 仍直接注入 IDbContextFactory<CloudPanDbContext> 并在 RefreshDataAsync 中裸跑 db.Devices.OrderByDescending(...).Take(20).ToListAsync() 查询设备列表，而 CloudPan.Server.Core/Status/ServerStatusService.GetDevicesAsync() 已提供完全相同的设备列表（AdminController/DevicesController 均复用该服务）；且以 IServiceProvider 从窗口构造注入（服务定位器）。
- **为何阻碍使命**：违背『UI 只渲染、领域数据访问进 Core』：同一查询在 Core 与 UI 双实现，改查询逻辑（过滤/排序/联表）需同步两处；UI 直接依赖 EF DbContext 使窗口无法脱离数据库独立测试，破坏分层单向依赖与『基础设施单一实现』收益。

### F-56 [架构 / P2] CloudPan.Server.Host/Controllers/FilesController.cs:92-105

- **问题**：FilesController.Upload 内嵌冲突检测业务策略：控制器直接调用 _index.GetByPathAsync 比较 baseVersion 与当前版本、决定是否调用 _fileOps.HandleUploadConflictAsync；而分块上传路径的冲突判定已下沉到 Core（ChunkedUploadService.FinalizeAsync）。两条上传路径的冲突判定一个在 HTTP 适配层、一个在领域服务，行为分叉。
- **为何阻碍使命**：违背『Controller 只做 HTTP 适配』且两端不一致：普通上传冲突策略留在控制器无法被服务级复用/单测，版本号/乐观并发语义变更需同时改 Controller 与 Core，与分块上传已下沉的先例矛盾，属『局部正确、全局错误』的典型残留。

### F-57 [架构 / P2] CloudPan.Server.Core/WebSocket/WebSocketHandler.Lifecycle.cs:43-96

- **问题**：Server.Core 的 WebSocketHandler 作为 Singleton 服务内置 System.Threading.Timer 心跳（CheckHeartbeats，PingInterval 周期触发），虽已用 Interlocked 防重入 + Task.Run 包裹符合 7.2，但仍属『裸 Timer 散落于领域服务』而非 IHostedService 统一托管；BackgroundHostedService 已集中 5 个定时任务，心跳却独立在外。
- **为何阻碍使命**：规则 6/7.6 要求定时任务用 IHostedService、禁止裸 Timer 散落；心跳这类生命周期型定时任务散落在 Singleton 服务里无法统一启停、无单点观察，且该 Singleton 已承载连接池+认证+广播+心跳多重职责。

### F-58 [技术简洁 / P2] CloudPan.Server.Host/Controllers/*.cs（16 处路由字面量）; CloudPan.Server.Host/Program.cs:111; CloudPan.Client.Core/Services/WebSocketClient.cs:51

- **问题**：SpecRoutes.g.cs 已生成全部 HTTP 路由常量且客户端 ApiClient 已引用（T-027），但服务端 16 处 Controller 路由模板仍手写字面量（HealthController/ShareController/FilesController 等），与 SpecRoutes 形成双源；"/ws" WebSocket 路径更完全未契约化，Program.cs 与 WebSocketClient 各拼一次。
- **为何阻碍使命**：契约驱动承诺只覆盖了客户端半边：spec 改路由时 CodeGen 更新 SpecRoutes，但 Controller 属性与 /ws 需手工同步，漏改即客户端契约与路由实现漂移，T-027 的收敛不完整。

### F-59 [功能 / P2] CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt:38-48; ui/FileListScreen.kt:159-191

- **问题**：getFileTree/getFileTreeInFolder 均传 limit=100，FileTreeResponse 的 nextCursor/hasMore 从未被 FileListScreen 使用：目录（或根）超过 100 个直接子项时列表静默截断（按 Path 排序取前 100），无任何提示；下拉刷新只是重拉同一前 100 条；根目录因先拉全树再 isDirectChild 过滤，实际展示直系子项更少。
- **为何阻碍使命**：『海量小文件/相册（数千照片）同步吞吐』是知识库点名的边界场景；列表截断让用户误以为云端只有这些文件，找不到文件等于功能不可用，且是 v1.0 声明浏览功能的主入口。

### F-60 [功能 / P2] CloudPan.Android/app/src/main/java/com/cloudpan/android/worker/PhotoBackupWorker.kt:167-175,180-182

- **问题**：照片备份队头阻塞（T-044 连续段游标的代价）：任何一张照片上传失败即 break 停止连续段，游标只推进到连续成功段末尾。若某张照片永久失败（文件损坏/权限拒绝/超服务端大小上限），每次 15 分钟运行都从该失败照片重新开始并再次失败，其后所有新照片永远无法备份；家庭相册数百张中混入一张坏照片即全相册停摆，且失败照片无『跳过/标记 Blocked』入口。
- **为何阻碍使命**：边界『海量小文件/相册』要求整库可推进；单张坏照片阻塞全部后续照片是把『不静默丢失』做成了『永久卡死』，失败可恢复判据在永久性失败上失效。

## 任务清单（本批次）

| ID | 优先级 | 维度 | 标题 | 依赖 | 验收方式 |
|----|--------|------|------|------|----------|
| T-049 | P1 | 功能 | 修复目录删除传播断裂（目录自身墓碑化 + FullScan 目录删除兜底） | - | 自动,自动,自动,自动 |
| T-050 | P1 | UX | Android 删除改走回收站（软删 + 撤销 Snackbar + 回收站入口） | - | 自动,手动,手动 |
| T-051 | P1 | 技术简洁 | 提交新版本编排收敛为单一领域辅助（Upload/ChunkedUpload 共用） | - | 自动,自动,自动,自动 |
| T-052 | P1 | 技术简洁 | 进程执行辅助收敛为单一 ProcessRunner | - | 自动,手动,自动 |
| T-053 | P2 | 技术简洁 | SettingsForm 测试连接回炉：改走 IApiClient + 白话反馈（T-041 残留） | - | 自动,自动,自动,手动 |
| T-054 | P2 | 功能 | 排除集语义闭环：覆盖上传方向 + 重新勾选恢复（T-047 补全） | - | 自动,自动,自动 |
| T-055 | P2 | 架构 | ServerWindow 改经 IServerStatusService（消除直连 DbContext 与服务定位器） | - | 自动,自动,自动 |
| T-056 | P2 | 架构 | FilesController.Upload 冲突检测下沉 Core | - | 自动,自动,自动 |
| T-057 | P2 | 架构 | WebSocket 心跳迁移 IHostedService（间隔读 SpecConfig） | - | 自动,自动,自动 |
| T-058 | P2 | 技术简洁 | 服务端路由字面量契约化 + /ws 路径生成 | - | 自动,自动,自动 |
| T-059 | P2 | 功能 | Android 文件列表分页（nextCursor 增量加载） | - | 自动,手动,手动 |
| T-060 | P2 | 功能 | 照片备份队头阻塞修复（失败重试上限 + 游标越过） | - | 自动,手动,手动 |

### T-049 修复目录删除传播断裂（目录自身墓碑化 + FullScan 目录删除兜底）（P1 / 功能）

- **来源**：F-49 @ CloudPan.Server.Core/FileIndex/FileIndexService.cs:129-136; CloudPan.Client.Core/Services/SyncEngine.FullScan.cs:100-118; CloudPan.Tests/Server/Services/FileIndexServiceTests.cs:88-109
- **范围**：FileIndexService.SoftDeleteAsync + SyncEngine.FullScan + 相关单测
- **依赖**：无
- **目标**：设备 A 删除目录后目录自身与后代均墓碑化并传播到其他设备，空目录不再在设备/索引中幽灵残留，删除后同名重建可成功
- **需求**：
  - SoftDeleteAsync 目录分支把目录自身条目纳入删除目标（prefix 匹配前显式把 path 加入 targets，或 Path==path OR Path LIKE 'path/%'），生产无尾斜杠路径下目录自身 FileEntry 被标记 Deleting
  - 客户端 FullScan 删除检测对目录快照补判定：本地目录缺失且快照为目录 → 入队 Delete，目录删除有 5 分钟兜底扫描（不再只依赖 FileSystemWatcher）
  - 目录墓碑化/清理后同名重建可成功（MkdirAsync 不再因已存在路径 409）
  - 单测对齐生产无尾斜杠约定：SoftDelete_文件夹_递归标记等改用无尾斜杠路径创建，删除掩盖不一致的测试
- **验收标准**：
  - [自动] 单测覆盖无尾斜杠目录软删：目录自身与子文件均被标记 Deleting（dotnet test --filter FileIndexService）
  - [自动] 单测覆盖目录删除兜底扫描：本地目录缺失且快照为目录 → 入队 Delete（dotnet test --filter SyncEngine）
  - [自动] 全量测试全绿（dotnet test CloudPan.Tests -c Release）
  - [自动] dotnet build 全绿（dotnet build CloudPan.sln -c Release）

### T-050 Android 删除改走回收站（软删 + 撤销 Snackbar + 回收站入口）（P1 / UX）

- **来源**：F-50 @ CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt:128-133; ui/FileListScreen.kt:210-258
- **范围**：Android FileRepository + FileListScreen（删除/批量删除路径）
- **依赖**：无
- **目标**：Android 删除与 Windows 行为对齐——删除进回收站可恢复，手机误触不再永久丢文件
- **需求**：
  - 删除确认框与批量删除改调服务端回收站软删 API（/api/trash 软删端点），禁止直接 /api/files DELETE 物理删除
  - 删除后显示 Snackbar『已删除，可撤销』并提供撤销动作（调用恢复端点），与 Windows 行为对齐
  - 增加回收站入口（最近删除列表 + 恢复/清空），确认文案白话化（『将移入回收站，可在回收站恢复』），杜绝不可逆事故
- **验收标准**：
  - [自动] FileRepository 无直接物理删除调用（删除改经回收站软删端点）（grep -rn 'deleteFile\|/api/files' CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt 无物理删除调用（删除走 trash 软删））
  - [手动] Android gradle compileDebugKotlin 编译通过
  - [手动] 手动：删除文件后出现『已删除，可撤销』Snackbar，撤销可恢复；回收站入口可见并列出已删文件

### T-051 提交新版本编排收敛为单一领域辅助（Upload/ChunkedUpload 共用）（P1 / 技术简洁）

- **来源**：F-51 @ CloudPan.Server.Core/Upload/UploadService.cs:52-76 vs CloudPan.Server.Core/ChunkedUpload/ChunkedUploadService.Finalize.cs:86-133
- **范围**：Server.Core 上传/分块/恢复 新版本提交路径
- **依赖**：无
- **目标**：提交新版本的一致性关键逻辑单一实现，任一修订不再双处同步，DB+FS 一致性由单点守护
- **需求**：
  - 抽取单一『提交新版本』领域辅助（入参：FileEntry 更新、存档内容、裁剪策略），统一事务边界 + 回滚 + FS 副作用清理模板
  - UploadService 与 ChunkedUploadService.Finalize（及 Restore 涉及处）改为调用该辅助，删除各自重复编排，保持各自入口错误语义（UploadStorageException 等）不退化
  - 补单测：存档→裁剪→更新事务与回滚路径经辅助覆盖
- **验收标准**：
  - [自动] StoreVersionAsync/裁剪编排仅存在于单一辅助，两处调用（grep -n 'StoreVersionAsync\|RemoveRange' CloudPan.Server.Core/Upload/UploadService.cs CloudPan.Server.Core/ChunkedUpload/ChunkedUploadService.Finalize.cs 无重复编排实现（均经辅助））
  - [自动] 单测覆盖提交新版本事务与回滚（dotnet test --filter Upload）
  - [自动] 全量测试全绿（dotnet test CloudPan.Tests -c Release）
  - [自动] dotnet build 全绿（dotnet build CloudPan.sln -c Release）

### T-052 进程执行辅助收敛为单一 ProcessRunner（P1 / 技术简洁）

- **来源**：F-52 @ CloudPan.Server.UI/ServerInstaller.Process.cs:13-121; CloudPan.Server.UI/ServiceRestartHelper.cs:69-127
- **范围**：Server.UI 进程执行路径
- **依赖**：无
- **目标**：命令执行单点实现，安全敏感区收敛为单一审计面，参数转义/超时/错误收集语义不再各自演化
- **需求**：
  - 新建单一 ProcessRunner（入参：超时、是否 runas、是否重定向输出、是否经 cmd 管道），统一 启动→WaitForExit 超时→Kill→收集 stderr→错误截断 语义
  - ServerInstaller.RunExe/RunCmd 与 ServiceRestartHelper.RunElevatedSc/ServiceHasLegacyBinPathParam 改为调用 ProcessRunner，删除 4 处近似实现
  - 保持参数转义安全（ArgumentList 不用字符串拼接 cmd 参数），超时与错误截断行为不退化
- **验收标准**：
  - [自动] Server.UI 无重复进程执行实现（ProcessStartInfo/WaitForExit 仅经 ProcessRunner）（grep -rn 'ProcessStartInfo\|WaitForExit' CloudPan.Server.UI/ 仅经 ProcessRunner 实现）
  - [手动] 安装器服务安装/重启路径正常（手动验证）
  - [自动] 全量测试全绿 + dotnet build 全绿（dotnet test CloudPan.Tests -c Release && dotnet build CloudPan.sln -c Release）

### T-053 SettingsForm 测试连接回炉：改走 IApiClient + 白话反馈（T-041 残留）（P2 / 技术简洁）

- **来源**：F-53 @ CloudPan.Client.UI/UI/SettingsForm.AccountTab.cs:229-271
- **范围**：SettingsForm.TestConnection_Click + IApiClient
- **依赖**：无
- **目标**：T-041『客户端 HTTP 单一入口』承诺闭环，设置页测试结果与真实同步连接一致且反馈白话、不打断
- **需求**：
  - 删除手建 HttpClient 与魔数 "/api/health"，测试连接改走 IApiClient 探活方法（内部引用 SpecRoutes.Health），唯一证书/代理/超时装配点
  - 成功不再弹模态 MessageBox，改图标+文字反馈（复用 _connResultIcon）
  - 失败按 ErrorAttribution 风格白话归因（连不上→确认服务器已开机/服务运行；404→检查服务端地址），不透出裸状态码与底层异常原文
- **验收标准**：
  - [自动] SettingsForm.AccountTab 无手建 HttpClient（grep -n 'new HttpClient' CloudPan.Client.UI/UI/SettingsForm.AccountTab.cs 无匹配）
  - [自动] Client.UI 无 /api/health 字面量（grep -rn '"/api/health"' CloudPan.Client.UI/ 无匹配）
  - [自动] dotnet build 全绿（dotnet build CloudPan.sln -c Release）
  - [手动] 手动：测试连接成功仅图标+文字反馈（无模态弹窗）；失败显示白话原因与下一步
- **注**：T-041 回炉任务：批次 4 T-041 标记完成但被 UI 层绕开，同一 HttpClient 装配的契约收敛仍留第二入口（simplicity F-53 与 ux U-7 同源合并）。

### T-054 排除集语义闭环：覆盖上传方向 + 重新勾选恢复（T-047 补全）（P2 / 功能）

- **来源**：F-54 @ CloudPan.Client.Core/Services/SyncEngine.Scan.cs:172,188; SyncEngine.FullScan.cs:92-98,166,178; FileWatcherService.cs:106,111
- **范围**：SyncEngine 排除集过滤 + cloudOnlyPaths + FileWatcherService
- **依赖**：无
- **目标**：选择性同步勾选/取消为可逆闭环，排除目录不再是单向发布源，隐私文件不外传
- **需求**：
  - 上传入队入口（EnqueueLocalChangeAsync/FullScan 无快照分支/FileWatcherService.OnChanged）统一在入队前调用 IsPathSelected 过滤排除子树，排除目录内本地文件不再上传（由 cloudOnlyPaths 保留本地副本）
  - 重新勾选已排除目录：IsPathSelected 转 true 且本地缺失 → 入队下载；本地存在 → 恢复 State（重置 CloudOnly），不再永久卡 CloudOnly
  - 补单测：排除目录内新建/修改文件不入队上传；重新勾选后文件恢复同步
- **验收标准**：
  - [自动] 单测覆盖排除子树上传拦截与重新勾选恢复（dotnet test --filter SyncEngine）
  - [自动] 全量测试全绿（dotnet test CloudPan.Tests -c Release）
  - [自动] dotnet build 全绿（dotnet build CloudPan.sln -c Release）
- **注**：T-047 补全：排除集只拦下载方向 + 重新勾选永久卡 CloudOnly 同源合并（function F-2/F-3）。

### T-055 ServerWindow 改经 IServerStatusService（消除直连 DbContext 与服务定位器）（P2 / 架构）

- **来源**：F-55 @ CloudPan.Server.UI/ServerWindow.cs:23,37; ServerWindow.Data.cs:19
- **范围**：Server.UI/ServerWindow + DI 装配
- **依赖**：无
- **目标**：UI 层不再直连持久化，设备列表查询单一实现于 Core，窗口可脱离数据库独立测试
- **需求**：
  - ServerWindow 移除对 IDbContextFactory<CloudPanDbContext> 的直接注入与裸 ToListAsync 查询，改为注入 IServerStatusService 调用 GetDevicesAsync()（与 AdminController/DevicesController 复用同一服务）
  - 窗口构造改为显式注入所需服务，不再把整个 IServiceProvider 传入 UI（消除服务定位器）
- **验收标准**：
  - [自动] ServerWindow 无 DbContext 直接引用（grep -n 'IDbContextFactory\|DbContext' CloudPan.Server.UI/ServerWindow.cs CloudPan.Server.UI/ServerWindow.Data.cs 无匹配）
  - [自动] ServerWindow 无 IServiceProvider 字段（grep -n 'IServiceProvider' CloudPan.Server.UI/ServerWindow.cs 无匹配）
  - [自动] 架构测试全绿 + dotnet build 全绿（dotnet test --filter Architecture && dotnet build CloudPan.sln -c Release）

### T-056 FilesController.Upload 冲突检测下沉 Core（P2 / 架构）

- **来源**：F-56 @ CloudPan.Server.Host/Controllers/FilesController.cs:92-105
- **范围**：FilesController + IUploadService
- **依赖**：无
- **目标**：两条上传路径的冲突策略单一实现于 Core，行为一致、可服务级单测
- **需求**：
  - 把冲突判定与处理合并进 IUploadService.UploadAsync（参照 ChunkedUploadService 已实现模式），Controller 只透传 baseVersion 并映射 CONFLICT 错误体
  - 删除 Controller 内 _index.GetByPathAsync 比较 baseVersion 与直接调用 HandleUploadConflictAsync 的策略代码
  - 补单测：普通上传冲突路径（服务端版本 > baseVersion → 冲突副本/409 语义）与分块上传一致
- **验收标准**：
  - [自动] FilesController.Upload 无冲突判定策略代码（grep -n 'HandleUploadConflictAsync\|GetByPathAsync' CloudPan.Server.Host/Controllers/FilesController.cs 无匹配）
  - [自动] 单测覆盖普通上传冲突路径（dotnet test --filter Upload）
  - [自动] 架构测试全绿 + dotnet build 全绿（dotnet test --filter Architecture && dotnet build CloudPan.sln -c Release）

### T-057 WebSocket 心跳迁移 IHostedService（间隔读 SpecConfig）（P2 / 架构）

- **来源**：F-57 @ CloudPan.Server.Core/WebSocket/WebSocketHandler.Lifecycle.cs:43-96
- **范围**：Server.Core WebSocketHandler 心跳 + HostedService 装配
- **依赖**：无
- **目标**：心跳生命周期统一托管，领域服务无裸 Timer 散落，间隔配置单一来源
- **需求**：
  - 把 CheckHeartbeats 心跳定时从 WebSocketHandler 内裸 System.Threading.Timer 迁移为独立 IHostedService（或并入 BackgroundHostedService 统一调度），WebSocketHandler 只保留连接管理
  - 心跳间隔改读 SpecConfig 单源（PingInterval），禁止字面量
- **验收标准**：
  - [自动] Server.Core/WebSocket 无裸 Timer（grep -n 'System.Threading.Timer' CloudPan.Server.Core/WebSocket/ 无匹配）
  - [自动] 心跳间隔引用 SpecConfig（grep -n 'PingInterval' CloudPan.Server.Core/ 有 SpecConfig 引用）
  - [自动] 架构测试全绿 + dotnet build 全绿（dotnet test --filter Architecture && dotnet build CloudPan.sln -c Release）

### T-058 服务端路由字面量契约化 + /ws 路径生成（P2 / 技术简洁）

- **来源**：F-58 @ CloudPan.Server.Host/Controllers/*.cs（16 处）; CloudPan.Server.Host/Program.cs:111; CloudPan.Client.Core/Services/WebSocketClient.cs:51
- **范围**：Server.Host Controllers + CodeGen 路由/WsPath 常量
- **依赖**：无
- **目标**：服务端路由单一事实来源为 SpecRoutes 生成常量，/ws 契约化，T-027 收敛补全服务端半边
- **需求**：
  - Controller 路由属性（HttpGet/HttpPost/Route 等 16 处）改引用 SpecRoutes 常量，删除手写路径字面量
  - CodeGen 为 spec api.websocket.endpoint 生成 WsPath 常量，Program.cs 与 WebSocketClient 改引用，删除手拼 "/ws"
  - 补验证：Controllers 无手写路由字面量、/ws 无手拼（CodeGen --verify 覆盖）
- **验收标准**：
  - [自动] Controllers 路由属性无手写路径字面量（grep -rnE 'Http(Get|Post|Put|Delete)\("/[^"]+"\)|Route\("/[^"]+"\)' CloudPan.Server.Host/Controllers/ 无匹配或全部引用 SpecRoutes 常量）
  - [自动] 无手拼 /ws 字面量（grep -rn '"/ws"' CloudPan.Server.Host/Program.cs CloudPan.Client.Core/Services/WebSocketClient.cs 无匹配）
  - [自动] CodeGen --verify 一致 + dotnet build 全绿（dotnet run --project CloudPan.CodeGen -- --verify && dotnet build CloudPan.sln -c Release）

### T-059 Android 文件列表分页（nextCursor 增量加载）（P2 / 功能）

- **来源**：F-59 @ CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt:38-48; ui/FileListScreen.kt:159-191
- **范围**：Android FileRepository + FileListScreen
- **依赖**：无
- **目标**：大目录（数千照片）下 Android 浏览不静默截断，用户可浏览全部文件
- **需求**：
  - getFileTree/getFileTreeInFolder 分页请求，UI 滚动到底自动加载 nextCursor 并拼接已加载列表，hasMore 控制是否继续加载
  - 根目录改为先拉子目录再逐项翻页或等价方案，展示全部直系子项；空结果与 hasMore 状态分别提示
  - 补验证：目录超 100 子项时列表可翻到全部，无重复无丢项
- **验收标准**：
  - [自动] FileRepository 分页不固定 limit=100（支持 nextCursor/hasMore）（grep -n 'nextCursor\|hasMore\|limit' CloudPan.Android/app/src/main/java/com/cloudpan/android/data/FileRepository.kt 有分页游标驱动逻辑）
  - [手动] Android gradle compileDebugKotlin 编译通过
  - [手动] 手动：构造 >100 子项目录，滚动到底加载剩余项且不重复不丢项

### T-060 照片备份队头阻塞修复（失败重试上限 + 游标越过）（P2 / 功能）

- **来源**：F-60 @ CloudPan.Android/app/src/main/java/com/cloudpan/android/worker/PhotoBackupWorker.kt:167-175,180-182
- **范围**：Android PhotoBackupWorker + BackupLog
- **依赖**：无
- **目标**：相册混入一张坏照片不再全相册停摆，失败可恢复且游标可推进
- **需求**：
  - BackupLog 增加失败重试上限（如 3 次），超限标记 Blocked 状态并允许游标越过，通知提示用户处理坏照片
  - 或对失败照片单独维护『待重试集合』，新照片游标不被永久失败照片阻塞
  - 补验证：单张永久失败照片不阻塞其后新照片备份
- **验收标准**：
  - [自动] PhotoBackupWorker 无因单张失败永久停止（有失败重试上限/Blocked 越过逻辑）（grep -n 'Blocked\|retry\|maxRetry' CloudPan.Android/app/src/main/java/com/cloudpan/android/worker/PhotoBackupWorker.kt 有失败上限/越过逻辑）
  - [手动] Android gradle compileDebugKotlin 编译通过
  - [手动] 手动：构造一张永久失败照片，其后新照片仍被备份，失败照片标记 Blocked 有提示
