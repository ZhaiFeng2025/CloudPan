// AUTO-GENERATED from shared-spec.json
// 版本: 1.11.0  日期: 2026-08-04
// 源: api.endpoints[].clientMethod（C# 客户端方法骨架：HTTP 方法/路径/参数绑定）
// claude: do not edit this file directly — regenerate with: dotnet run --project CloudPan.CodeGen

#nullable enable

using System.Linq;
using System.Net.Http.Json;
using Microsoft.Extensions.Logging;
using CloudPan.Contract;

namespace CloudPan.Client.Core.Services;

/// <summary>
/// ApiClient 部分类：端点方法骨架。由 ApiClientGenerator 从 shared-spec.json → api.endpoints[].clientMethod 生成。
/// 复杂方法（上传限速/下载校验/分块编排/健康吞异常等）在 spec 标 manual 保留手工实现，签名由接口强约束。
/// </summary>
public partial class ApiClient
{
    /// <summary>
    /// 文件树（支持 sinceVersion/limit/cursor 分页）（GET /api/files/tree）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<FileTreeResponse?> GetFileTreeAsync(int sinceVersion, int limit = 5000, string? subPath = null, string? cursor = null, CancellationToken ct = default)
    {
        string url = $"{SpecRoutes.FilesTree}?sinceVersion={sinceVersion}&limit={limit}";
        if (!string.IsNullOrEmpty(subPath))
        {
            url += $"&path={Uri.EscapeDataString(subPath)}";
        }
        if (!string.IsNullOrEmpty(cursor))
        {
            url += $"&cursor={Uri.EscapeDataString(cursor)}";
        }
        var response = await _http.GetAsync(url, ct);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<FileTreeResponse>(JsonOptions, ct);
    }

    /// <summary>
    /// 删除文件/文件夹（递归）（POST /api/files/delete）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task DeleteAsync(string path, int baseVersion, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.FilesDelete, new DeleteRequestDto(path, baseVersion), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
    }

    /// <summary>
    /// 移动/重命名（POST /api/files/move）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task MoveAsync(string oldPath, string newPath, int baseVersion, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.FilesMove, new MoveRequestDto(oldPath, newPath, baseVersion), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
    }

    /// <summary>
    /// 创建文件夹（POST /api/files/mkdir）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task MkdirAsync(string path, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.FilesMkdir, new MkdirRequestDto(path), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
    }

    /// <summary>
    /// 图片缩略图（GET /api/thumbnails）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<byte[]?> GetThumbnailAsync(string path, int width, CancellationToken ct = default)
    {
        string url = $"{SpecRoutes.Thumbnails}?path={Uri.EscapeDataString(path)}&width={width}";
        try
        {
            var response = await _http.GetAsync(url, ct);
            if (!response.IsSuccessStatusCode)
            {
                _logger?.LogWarning("二进制端点失败: {Url} → {Status}", url, response.StatusCode);
                return null;
            }
            return await response.Content.ReadAsByteArrayAsync(ct);
        }
        catch (Exception ex)
        {
            _logger?.LogWarning(ex, "二进制端点异常: {Url}", url);
            return null;
        }
    }

    /// <summary>
    /// 版本历史列表（GET /api/versions）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<List<VersionItem>> GetVersionsAsync(string path, int limit = 50, CancellationToken ct = default)
    {
        string url = $"{SpecRoutes.Versions}?path={Uri.EscapeDataString(path)}&limit={limit}";
        var response = await _http.GetAsync(url, ct);
        response.EnsureSuccessStatusCode();
        var result = await response.Content.ReadFromJsonAsync<VersionListResponse>(JsonOptions, ct);
        return result?.Data?.ToList() ?? new List<VersionItem>();
    }

    /// <summary>
    /// 回滚到历史版本（POST /api/versions/restore）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<VersionRestoreResponse?> RestoreVersionAsync(string filePath, int version, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.VersionsRestore, new RestoreRequestDto(filePath, version), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<VersionRestoreResponse>(JsonOptions, ct);
    }

    /// <summary>
    /// 创建分享链接（POST /api/shares）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<ShareCreateResponse?> CreateShareAsync(string filePath, string? password = null, string? expiresAt = null, int? maxDownloads = null, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.Shares, new CreateShareRequestDto(filePath, password, expiresAt, maxDownloads), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<ShareCreateResponse>(JsonOptions, ct);
    }

    /// <summary>
    /// 分享链接列表（当前设备创建，不含 token 等敏感字段）（GET /api/shares）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<List<ShareListItem>> GetSharesAsync(CancellationToken ct = default)
    {
        var response = await _http.GetAsync(SpecRoutes.Shares, ct);
        response.EnsureSuccessStatusCode();
        var result = await response.Content.ReadFromJsonAsync<ShareListResponse>(JsonOptions, ct);
        return result?.Data?.ToList() ?? new List<ShareListItem>();
    }

    /// <summary>
    /// 撤销分享链接（DELETE /api/shares/{shareId}）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<bool> RevokeShareAsync(string shareId, CancellationToken ct = default)
    {
        string url = SpecRoutes.SharesByShareId.Replace("{shareId}", Uri.EscapeDataString(shareId));
        var response = await _http.DeleteAsync(url, ct);
        if (response.StatusCode == System.Net.HttpStatusCode.NotFound)
        {
            return false;
        }
        response.EnsureSuccessStatusCode();
        return true;
    }

    /// <summary>
    /// 回收站文件列表（GET /api/trash）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task<List<TrashItem>> GetTrashAsync(CancellationToken ct = default)
    {
        var response = await _http.GetAsync(SpecRoutes.Trash, ct);
        response.EnsureSuccessStatusCode();
        var result = await response.Content.ReadFromJsonAsync<TrashListResponse>(JsonOptions, ct);
        return result?.Data?.ToList() ?? new List<TrashItem>();
    }

    /// <summary>
    /// 恢复回收站文件（POST /api/trash/restore）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task RestoreTrashAsync(string metaFileName, CancellationToken ct = default)
    {
        var response = await _http.PostAsJsonAsync(SpecRoutes.TrashRestore, new RestoreTrashRequestDto(metaFileName), JsonOptions, ct);
        response.EnsureSuccessStatusCode();
    }

    /// <summary>
    /// 清空回收站（DELETE /api/trash/empty）。由 shared-spec.json → api.endpoints 生成。
    /// </summary>
    public async Task EmptyTrashAsync(CancellationToken ct = default)
    {
        var response = await _http.DeleteAsync(SpecRoutes.TrashEmpty, ct);
        response.EnsureSuccessStatusCode();
    }

}
